FUNC void save_array() {
int array_length;
int idx;
array_length = 100;

  exec("log > saw.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",saw[idx]);
  }
  exec("log off");
}
