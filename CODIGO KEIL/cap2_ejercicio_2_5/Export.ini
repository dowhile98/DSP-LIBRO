FUNC void save_array() {
int array_length;
int idx;
array_length = 10;

  exec("log > x1.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",x1[idx]);
  }
  exec("log off");
  
  exec("log > x2.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%d\n",x2[idx]);
  }
  exec("log off");
  
  exec("log > x3.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%d\n",x3[idx]);
  }
  exec("log off");
  
  exec("log > x4.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",x4[idx]);
  }
  exec("log off");
  
  exec("log > x5.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",x5[idx]);
  }
  exec("log off");
}
