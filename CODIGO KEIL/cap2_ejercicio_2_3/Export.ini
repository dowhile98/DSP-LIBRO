FUNC void save_array() {
int array_length;
int idx;
array_length = 2000;

  exec("log > acc_buffer.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%d\n",acc_buffer[idx]);
  }
  exec("log off");  
}
