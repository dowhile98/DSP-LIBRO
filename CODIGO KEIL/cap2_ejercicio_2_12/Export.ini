FUNC void save_array() {
int array_length;
int idx;
array_length = 10;

  exec("log > y1.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",y1[idx]);
  }
  exec("log off");
  
  exec("log > y2.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",y2[idx]);
  }
  exec("log off");
  
  exec("log > y3.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",y3[idx]);
  }
  exec("log off");
  
  exec("log > y4.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n",y4[idx]);
  }
  exec("log off");
}
